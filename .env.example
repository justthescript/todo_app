# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Secret (change this to a secure random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Email Configuration (for password reset)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=Todo App <noreply@todoapp.com>

# Frontend URL (for password reset links)
FRONTEND_URL=http://localhost:3000

# Database (PostgreSQL)
# For local development, you can use a local PostgreSQL instance or a free service like Supabase
# For Render, this will be automatically provided when you add a PostgreSQL database
DATABASE_URL=postgresql://username:password@localhost:5432/todo_app
